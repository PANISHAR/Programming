#include <bits/stdc++.h>
using namespace std;

int32_t main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    // freopen ("input_custom.txt", "r", stdin);
    // freopen ("output_custom.txt", "w", stdout);

    vector<int> v(10);
    for (int ele : v)
    {
        v.push_back(ele);
    }
    for (auto ele : v)
    {
        cout << ele << " ";
    }
    cout << endl;

    return 0;
}